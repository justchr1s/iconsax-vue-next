// Test setup file
// Add any global test configuration here

import { config } from '@vue/test-utils'

// Global test configuration
config.global.stubs = {}

// Suppress Vue warnings in tests if needed
// config.global.config.warnHandler = () => {}
